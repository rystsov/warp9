/*

Element:
  	bindto : Element->void
  		указывает, что текущий элемент следует за Element и отрисовывает себя
  		вызывается один раз
    place : HTMLElement->void
    	при нескольких вызовах следующий HTMLElement размещается перед предыдущим
    	умеет отрисовывать HTMLElement строго за собой
*/